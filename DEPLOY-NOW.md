# Publicação final

O projeto Vercel `v4-gt-tracking-agent` já existe. Use o diretório `vercel-safe/` como raiz do deploy.

## 1. Crie um repositório privado no GitHub

Nome: `v4-gt-tracking-agent`

Envie somente o conteúdo da pasta `vercel-safe/`. Não envie `PRIVATE-SETUP.txt`.

## 2. Conecte o repositório ao projeto Vercel existente

Projeto: `v4-gt-tracking-agent`
Project ID: `prj_siDNdvvq6eQO6p3BvIZM9wTMAejz`

## 3. Cadastre as cinco variáveis

Copie os valores de `PRIVATE-SETUP.txt` para Settings > Environment Variables.

## 4. Faça o deploy

Depois do deploy, teste `/health/<MCP_ACCESS_TOKEN>` e conecte `/mcp/<MCP_ACCESS_TOKEN>` ao ChatGPT Developer Mode.
